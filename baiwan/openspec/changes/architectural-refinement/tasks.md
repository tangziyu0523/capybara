## 1. Implementation
- [ ] 1.1 Update `DistrictBuilder.js`: `createCurvedCabinGeometry` to use dynamic scaling based on volume/standard unit size.
- [ ] 1.2 Update `DistrictBuilder.js`: Replace `createOrganicCoralGeometry` with new logic implementing the Ellipsoid/Cube mix.
- [ ] 1.3 Refine color palette in `DistrictBuilder.constructor`.

## 2. Verification
- [ ] 2.1 Visual Check: Forest cabins should be roughly similar in mass to commercial buildings.
- [ ] 2.2 Visual Check: Tidal district should show clean geometric shapes (spheres/cubes) instead of "lumpy" coral.
- [ ] 2.3 Visual Check: Colors should be distinct but harmonious.
