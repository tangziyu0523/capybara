# Implementation Tasks

- [ ] **1. Git Setup**
  - [ ] Create/Update root `.gitignore`.
  - [ ] Initialize Git repository (if not exists).
  - [ ] Stage all files.

- [ ] **2. Docker Configuration**
  - [ ] Create `million-cubic-city/Dockerfile`.
  - [ ] Create `million-cubic-city/nginx.conf`.
  - [ ] Create `million-cubic-city/.dockerignore`.

- [ ] **3. Documentation**
  - [ ] Create `DEPLOY.md` at root with "Push to GitHub" and "Deploy to Tencent Cloud" instructions.

- [ ] **4. Validation**
  - [ ] Verify `git status` is clean (ignoring correct files).
  - [ ] (Optional) Verify Docker build locally if Docker is available.
